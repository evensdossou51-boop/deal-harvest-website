[build]
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"

# Optional: local dev redirects for clean function path
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
